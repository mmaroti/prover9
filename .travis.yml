language: c

matrix:
  include:
    - os: linux
      arch: amd64
      dist: focal
      compiler: gcc
    - os: linux
      arch: amd64
      dist: focal
      compiler: clang
    - os: linux
      arch: arm64
      dist: focal
      compiler: gcc
    - os: linux
      arch: amd64
      dist: bionic
      compiler: gcc
    - os: linux
      arch: amd64
      dist: xenial
      compiler: gcc
    - os: linux
      arch: amd64
      dist: trusty
      compiler: gcc
    - os: osx
      compiler: clang
    - os: windows
      script:
        - mkdir -p build
        - cd build
        - cmake -G "NMake Makefiles" ..
        - dir
        - mingw32-make
        - nmake
        - cmake ..
        - msbuild prover9.sln

script:
  - mkdir -p build
  - cd build
  - cmake ..
  - make -j

sudo: false

notifications:
  email: false
